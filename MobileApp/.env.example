# ============================================================
# Cardose Premium Gift Box - Deployment Configuration
# ============================================================
# Copy this file to .env and fill in your values.
# This single file configures the entire stack.
# ============================================================

# --- REQUIRED ---

# JWT Secret (generate a random 64-char string)
# Example: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_TO_A_RANDOM_64_CHAR_STRING

# --- DEPLOYMENT SCENARIO ---

# Your server's address as seen by clients.
# For local PC:      http://localhost
# For LAN server:    http://192.168.x.x
# For cloud VPS:     https://cardose.yourdomain.com
SERVER_URL=http://localhost

# Port for the backend API (internal)
API_PORT=3001

# Port for the web dashboard (what users type in browser)
WEB_PORT=80

# --- CORS ---

# Allowed origins for API requests (comma-separated).
# Should include your SERVER_URL and any other origins.
# For local:    http://localhost,http://localhost:80
# For LAN:      http://192.168.1.100,http://192.168.1.100:80
# For cloud:    https://cardose.yourdomain.com
CORS_ORIGIN=http://localhost,http://localhost:80

# --- DATABASE ---

DATABASE_PATH=./data/premiumgiftbox.db

# --- OPTIONAL: WhatsApp Business API ---

WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_APP_SECRET=
WHATSAPP_VERIFY_TOKEN=premium-gift-box-webhook-token

# --- OPTIONAL: Email/SMTP ---

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@premiumgiftbox.com

# --- OPTIONAL: Backups ---

BACKUP_DIR=./backups
BACKUP_FREQUENCY=4
AUTO_BACKUP=true

# --- OPTIONAL: HTTPS (Cloud VPS only) ---

# Your domain name (required for automatic HTTPS via Caddy)
# Leave empty for local/LAN deployment (no HTTPS needed)
DOMAIN=

# Your email for Let's Encrypt certificate notifications
LETSENCRYPT_EMAIL=
