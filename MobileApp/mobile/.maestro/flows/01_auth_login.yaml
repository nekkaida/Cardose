# Authentication Flow Tests
# Tests login functionality

appId: com.cardose.premiumgiftbox
name: Authentication - Login Flow
---

# Launch the app
- launchApp

# Test: Login with valid credentials
- assertVisible: "Login"
- assertVisible:
    text: "Username"
- assertVisible:
    text: "Password"

# Enter credentials
- tapOn:
    text: "Username"
- inputText: "manager"

- tapOn:
    text: "Password"
- inputText: "manager123"

# Submit login
- tapOn:
    text: "Login"

# Wait for navigation
- waitForAnimationToEnd

# Verify successful login - should see main tab
- assertVisible:
    text: "Cardose"

# Should see user info
- assertVisible:
    text: "manager"
